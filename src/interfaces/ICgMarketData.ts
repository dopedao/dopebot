interface ICgMarketData {
    prices: [
        [number, number]
    ],
    total_volumes: [
        [number, number]
    ],
    total_marketcaps: [
        [number, number]
    ]
}

export default ICgMarketData;
