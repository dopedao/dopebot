module.exports = {
    name: "roadmap",
    description: `Tells you the roadmap`,
    async execute(message, [option]) {
        switch(option) {
            default:
                break;
        }
    }
};