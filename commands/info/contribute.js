module.exports = {
    name: "contribute",
    description: `Some helpful tips to contribute :)`,
    async execute(message, [option]) {
        switch(option) {
            default:
                break;
        }
    }
};